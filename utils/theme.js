import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4ECDC4",
    },
    secondary: {
      light: "#383e44",
      main: "#292f36",
    },
    error: {
      main: "#FF6B6B",
    },
  },
});

export default theme;

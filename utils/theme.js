import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4ECDC4",
    },
    secondary: {
      light: "#BDC2C8",
      main: "#292f36",
    },
    error: {
      main: "#FF6B6B",
    },
  },
});

export default theme;
